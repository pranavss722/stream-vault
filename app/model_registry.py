"""Model registry — loads and serves champion/challenger models via MLflow."""

# TODO: Implement ModelRegistry class
# - load_model(model_name, stage) -> model
# - get_champion() -> model
# - get_challenger() -> model | None
