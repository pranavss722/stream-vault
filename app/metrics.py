"""Prometheus metrics — latency histograms, error counters, drift gauges."""

# TODO: Define and expose Prometheus metrics
# - prediction_latency_seconds (Histogram)
# - prediction_errors_total (Counter)
# - drift_score (Gauge)
